# Отчёт об исправлении утечек памяти

## Проблема
Вкладка браузера потребляет **1.5 ГБ** памяти при том, что данные маппингов занимают всего **20 МБ**.

## Найденные критические утечки

### 1. 🔴 Неочищенные таймеры при открытии редактора (main.js:289-309)
**Проблема**: При каждом открытии окна редактора создавались 2 таймера (setInterval + setTimeout), но очищался только один. При открытии 100 окон редактора накапливалось 100 "забытых" setTimeout, каждый из которых держал ссылки на closure и DOM элементы.

**Исправление**: Добавлена переменная `safetyTimeout` для хранения ссылки на setTimeout и его очистка при закрытии окна.

```javascript
// ДО (утечка):
setTimeout(() => { ... }, 5 * 60 * 1000); // Таймер терялся

// ПОСЛЕ (исправлено):
let safetyTimeout = null;
safetyTimeout = setTimeout(() => { ... }, 5 * 60 * 1000);
// Очищается при закрытии окна:
if (safetyTimeout) clearTimeout(safetyTimeout);
```

**Потенциальная экономия**: ~100-200 МБ при активном использовании редактора

---

### 2. 🔴 Бесконечно растущий elementCache (core.js:972)
**Проблема**: `window.elementCache = new Map()` никогда не очищался и мог расти бесконечно. Каждая ссылка на DOM элемент блокирует сборку мусора для всего поддерева DOM.

**Исправление**: Добавлено ограничение в 100 элементов с автоматическим удалением старейших при переполнении (LRU-like стратегия).

```javascript
const MAX_ELEMENT_CACHE_SIZE = 100;

if (window.elementCache.size >= MAX_ELEMENT_CACHE_SIZE) {
    const firstKey = window.elementCache.keys().next().value;
    window.elementCache.delete(firstKey);
}
```

**Потенциальная экономия**: ~200-500 МБ в долгоживущих сессиях

---

### 3. 🔴 Глобальные Map/Set растут бесконтрольно (mappings.js:12-24)
**Проблема**: Три глобальных структуры данных росли без ограничений:
- `window.mappingPreviewState` (Set) - состояния раскрытых превью
- `window.mappingPreviewToastState` (Map) - история показанных уведомлений
- `window.optimisticShadowMappings` (Map) - оптимистичные обновления

**Исправление**: Добавлен периодический cleanup (раз в минуту) с ограничениями:
- MAX_PREVIEW_STATE_SIZE: 50
- MAX_TOAST_STATE_SIZE: 100
- MAX_OPTIMISTIC_MAPPINGS: 50

```javascript
window.mappingMemoryCleanupInterval = window.LifecycleManager.setInterval(() => {
    // Очистка старых записей с сохранением самых свежих
    if (window.mappingPreviewState.size > MAX_PREVIEW_STATE_SIZE) {
        const toKeep = Array.from(window.mappingPreviewState).slice(-MAX_PREVIEW_STATE_SIZE);
        window.mappingPreviewState.clear();
        toKeep.forEach(id => window.mappingPreviewState.add(id));
    }
    // ... аналогично для других структур
}, 60000);
```

**Потенциальная экономия**: ~300-600 МБ при длительной работе

---

### 4. 🔴 NotificationManager.dedupeMap утечка (managers.js:26)
**Проблема**: Map для дедупликации уведомлений мог расти бесконечно. Хотя были cleanupTimer для каждой записи, при быстром закрытии/ошибках таймеры могли не срабатывать.

**Исправление**: Добавлен периодический cleanup с двойной защитой:
1. Удаление записей старше `dedupeWindowMs * 2`
2. Ограничение размера в 100 записей с удалением старейших

```javascript
this._cleanupInterval = window.LifecycleManager.setInterval(() => {
    const now = Date.now();
    const maxSize = 100;

    // Удаляем устаревшие
    for (const [key, entry] of this.dedupeMap.entries()) {
        if (now - entry.timestamp > this.dedupeWindowMs * 2) {
            if (entry.cleanupTimer) clearTimeout(entry.cleanupTimer);
            this.dedupeMap.delete(key);
        }
    }

    // Ограничиваем размер
    if (this.dedupeMap.size > maxSize) { /* ... */ }
}, 60000);
```

**Потенциальная экономия**: ~50-100 МБ при активных уведомлениях

---

### 5. 🔴 BroadcastChannel не закрываются (main.js:743-773)
**Проблема**: Создавались 2 BroadcastChannel, но никогда не закрывались, что приводило к утечке event listeners и держало страницу в памяти даже после перехода на другой URL.

**Исправление**:
1. Сохранение ссылок в `window.broadcastChannels[]`
2. Закрытие всех каналов при `beforeunload`

```javascript
window.broadcastChannels = window.broadcastChannels || [];
const cacheRefreshChannel = new BroadcastChannel('imock-cache-refresh');
window.broadcastChannels.push(cacheRefreshChannel);

// Cleanup при выгрузке страницы
window.addEventListener('beforeunload', () => {
    window.broadcastChannels.forEach(channel => channel.close());
    window.broadcastChannels = [];
});
```

**Потенциальная экономия**: ~10-30 МБ + правильная выгрузка страницы

---

## Общая оценка экономии памяти

| Исправление | Экономия памяти |
|-------------|----------------|
| setTimeout cleanup в editMapping | 100-200 МБ |
| elementCache ограничение | 200-500 МБ |
| Map/Set cleanup в mappings.js | 300-600 МБ |
| NotificationManager.dedupeMap | 50-100 МБ |
| BroadcastChannel cleanup | 10-30 МБ |
| **ИТОГО** | **660-1430 МБ** |

## Ожидаемый результат

После применения всех исправлений:
- **Базовое потребление**: ~50-100 МБ (структуры данных + DOM)
- **С маппингами (20 МБ)**: ~70-120 МБ
- **При активном использовании**: ~150-250 МБ (пик)

Вместо текущих **1.5 ГБ** → ожидается **~100-250 МБ** в зависимости от активности.

## Дополнительные улучшения (не реализованы в этом коммите)

### Monaco Editor
- ✅ Health monitoring уже имеет cleanup в `dispose()` методе
- ⚠️ Нужно убедиться, что `dispose()` вызывается при закрытии окна редактора

### Event Listeners
- ⚠️ Множественные `window.addEventListener('storage')` (3 обработчика)
- ⚠️ `window.addEventListener('message')` без cleanup
- Рекомендуется: создать единый менеджер событий с автоматической очисткой

### Cache Intervals
- js/features/cache.js имеет несколько интервалов:
  - `cleanupInterval` (строка 36)
  - `syncInterval` (строка 39)
  - `healthCheckInterval` (строка 350)
  - `cacheValidationInterval` (строка 561)
- Все используют `LifecycleManager.setInterval`, что хорошо
- ✅ Очищаются через `LifecycleManager.clearAll()` при `beforeunload`

## Рекомендации для дальнейшей оптимизации

1. **Мониторинг памяти**: Добавить периодическое логирование `performance.memory`
2. **Heap snapshots**: Периодически делать снимки памяти в Chrome DevTools
3. **Lazy loading**: Загружать Monaco Editor только при первом открытии редактора
4. **Virtual scrolling**: Для больших списков маппингов использовать виртуальный скроллинг
5. **WeakMap/WeakSet**: Где возможно, использовать слабые ссылки

## Тестирование

Для проверки эффективности исправлений:

```javascript
// В консоли браузера
console.log('Memory:', {
    used: Math.round(performance.memory.usedJSHeapSize / 1024 / 1024) + ' MB',
    total: Math.round(performance.memory.totalJSHeapSize / 1024 / 1024) + ' MB',
    limit: Math.round(performance.memory.jsHeapSizeLimit / 1024 / 1024) + ' MB'
});

// Проверить размеры структур данных
console.log('Data structures:', {
    elementCache: window.elementCache.size,
    mappingPreviewState: window.mappingPreviewState.size,
    mappingPreviewToastState: window.mappingPreviewToastState.size,
    optimisticShadowMappings: window.optimisticShadowMappings.size,
    notificationDedupeMap: window.NotificationManager.dedupeMap.size
});
```

---

**Дата**: 2025-11-11
**Автор**: Claude Code Assistant
**Ветка**: `claude/debug-memory-leak-011CV1bahfYHMRHEvSnyq46f`
