{
  "name": "imock-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "generate:sdk": "npx -p @openapitools/openapi-generator-cli openapi-generator-cli generate -g typescript-fetch -o ./packages/wiremock-sdk -i %WM_OAS_URL% --additional-properties=supportsES6=true,typescriptThreePlus=true,npmName=wiremock-sdk",
    "bootstrap": "npm --prefix packages/wiremock-sdk-extra install && npm --prefix apps/wiremock-admin-ui install",
    "build": "npm --prefix packages/wiremock-sdk-extra run build && npm --prefix apps/wiremock-admin-ui run build",
    "dev": "npm --prefix apps/wiremock-admin-ui run dev"
  }
}
