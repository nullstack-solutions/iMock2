{
  "name": "imock2",
  "version": "1.0.0",
  "description": "WireMock UI - Modern web interface for WireMock mock server",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "npx serve -l 53771",
    "test": "node tests/run-all.js",
    "coverage": "rm -rf coverage && NODE_V8_COVERAGE=coverage node tests/run-all.js && npx --yes c8 report --reporter=text-summary --temp-directory=coverage"
  },
  "keywords": [
    "wiremock",
    "mock",
    "testing",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "eslint": "^9.39.1"
  }
}
